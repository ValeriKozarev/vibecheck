import React from 'react';

// stylized div to show footer containing additional info about the app
const FooterSection: React.FunctionComponent = (): JSX.Element => {
    return(
        <div className="footer">
            <p>made by val</p>
        </div>
    );
}

export default FooterSection;
